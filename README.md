# Fedora 33 post install

Make sure to install Fedora 33 with BTRFS file system and subvolume layout:
- label(\) == @
- label(\home) == @home

Then launch the following three scripts. Lastly follow instructions of script four.
